<div class="errors">
<% if !@post.errors.messages.empty?%>
  <% @post.errors.full_messages.each do |error| %>
    <p1><%= error %> </p1> <br>
  <%end%>
<% end %>
</div>
<%= link_to "Back", user_posts_path(current_user), class:"back-link-50"%>
<%= render 'new_post_form' %>
